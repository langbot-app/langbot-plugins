name: LocalAgent tests

on:
  push:
    paths:
      - "Runner/LocalAgent/**"
      - ".github/workflows/local-agent-tests.yml"
  pull_request:
    paths:
      - "Runner/LocalAgent/**"
      - ".github/workflows/local-agent-tests.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout plugins
        uses: actions/checkout@v4
        with:
          path: langbot-plugins


      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Keep environment outside the checkout
        run: |
          printf 'UV_PROJECT_ENVIRONMENT=%s/local-agent-venv\n' "$RUNNER_TEMP" >> "$GITHUB_ENV"

      - name: Install dependencies
        run: uv sync --locked --extra dev
        working-directory: langbot-plugins/Runner/LocalAgent

      - name: Verify published SDK pin
        run: uv run --no-sync python -c 'from importlib.metadata import version; assert version("langbot-plugin") == "0.6.1"; print("langbot-plugin", version("langbot-plugin"))'
        working-directory: langbot-plugins/Runner/LocalAgent

      - name: Ruff check
        run: uv run --no-sync ruff check .
        working-directory: langbot-plugins/Runner/LocalAgent

      - name: Ruff format check
        run: uv run --no-sync ruff format --check .
        working-directory: langbot-plugins/Runner/LocalAgent

      - name: Pytest
        run: uv run --no-sync pytest
        working-directory: langbot-plugins/Runner/LocalAgent
